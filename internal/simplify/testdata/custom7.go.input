package demo

import (
	"log"
	"time"
)

func doTimeSub() {
	n1:=time.Now()
	s1:=time.Now().Sub(n1)
	log.Println(s1)

	if time.Now().Sub(n).Seconds()>0{
        log.Println("ok")
    }
}

func doSubNow(){
	n:=time.Now()
	a:=n.Sub(time.Now())
	b:=time.Until(n)
	log.Println(a,b)
	
	if n.Sub(time.Now()).Seconds()>0{
		log.Println("ok")
	}
}
